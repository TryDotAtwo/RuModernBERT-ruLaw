[project]
name = "legal-modernbert-training"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "accelerate>=0.34.0",
  "datasets>=2.20.0",
  "torch>=2.4.0",
  "transformers>=4.48.0",
  "deepspeed>=0.14.5",
  "tensorboard>=2.17.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
