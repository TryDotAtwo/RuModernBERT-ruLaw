[project]
name = "legal-modernbert-training"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "accelerate>=0.34.0",
  "datasets>=2.20.0",
  "flash-attn>=2.6.0",
  "torch>=2.4.0",
  "transformers>=4.48.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
